# Visual Project Guide

## 🏗️ System Architecture Diagram

```
┌────────────────────────────────────────────────────────────────────┐
│                          END USERS                                 │
├────────────────────────────────────────────────────────────────────┤
│  Developers  │  DevOps Teams  │  Security Teams  │  Management     │
└──────┬───────┴──────┬─────────┴──────┬──────────┴──────┬───────────┘
       │              │                │                 │
       ▼              ▼                ▼                 ▼
┌──────────────┬──────────────┬──────────────┬──────────────┐
│ VS Code Ext  │   Web UI     │    CLI       │    API       │
│  (Real-time) │ (Dashboard)  │  (Scripts)   │  (Webhooks)  │
└──────┬───────┴──────┬───────┴──────┬───────┴──────┬───────┘
       │              │              │              │
       └──────────────┼──────────────┼──────────────┘
                      ▼
        ┌─────────────────────────────┐
        │    API Gateway / Load       │
        │    Balancer (NGINX)         │
        └──────────┬──────────────────┘
                   │
       ┌───────────┼───────────────────────┐
       │           │                       │
       ▼           ▼                       ▼
   ┌────────┐  ┌──────────┐           ┌─────────┐
   │Backend │  │Frontend  │           │ML/AI    │
   │Node.js │  │React     │           │Python   │
   │:5000   │  │:3000     │           │:5001    │
   └────┬───┘  └────┬─────┘           └────┬────┘
        │           │                      │
        └───────────┼──────────────────────┘
                    │
        ┌───────────┴──────────────┐
        │                          │
        ▼                          ▼
    ┌─────────────┐          ┌─────────────┐
    │  MongoDB    │          │  Redis      │
    │  Database   │          │  Cache      │
    └─────────────┘          └─────────────┘
```

## 📊 Data Flow Diagram

```
┌─────────────┐
│   User      │
│  Initiates  │
│   Scan      │
└──────┬──────┘
       │
       ▼
┌──────────────────────┐
│  Frontend            │
│  (React Dashboard)   │
│  - Displays UI       │
│  - Captures Input    │
└──────┬───────────────┘
       │
       │ HTTP Request
       │
       ▼
┌──────────────────────┐
│  Backend API         │
│  (Node.js/Express)   │
│  - Receives Request  │
│  - Validates Input   │
│  - Queues Scan Task  │
└──────┬───────────────┘
       │
       │ Task Message
       │
       ▼
┌──────────────────────┐
│  ML/AI Engine        │
│  (Python/Flask)      │
│  - Analyzes Code     │
│  - Detects Vulns     │
│  - Calculates Score  │
└──────┬───────────────┘
       │
       │ Results
       │
       ▼
┌──────────────────────┐
│  Backend API         │
│  - Stores Results    │
│  - Updates Status    │
│  - Broadcasts Update │
└──────┬───────────────┘
       │
       │ WebSocket/SSE
       │
       ▼
┌──────────────────────┐
│  Frontend Dashboard  │
│  - Updates UI        │
│  - Shows Results     │
│  - Displays Charts   │
└──────────────────────┘
```

## 🔄 Feature Development Workflow

```
┌─────────────────────────────────────────────┐
│  1. Choose Feature from Roadmap             │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  2. Create Feature Branch                   │
│     git checkout -b feature/your-feature    │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  3. Backend Development                     │
│  ├─ Create route                            │
│  ├─ Implement controller                    │
│  ├─ Add business logic                      │
│  └─ Write tests                             │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  4. Frontend Development                    │
│  ├─ Create component                        │
│  ├─ Connect to API                          │
│  ├─ Add styling                             │
│  └─ Write tests                             │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  5. Testing                                 │
│  ├─ Unit tests pass                         │
│  ├─ Integration tests pass                  │
│  ├─ Manual testing completed                │
│  └─ No console errors                       │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  6. Documentation Update                    │
│  ├─ Update README if needed                 │
│  ├─ Add code comments                       │
│  └─ Update API docs                         │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  7. Code Review                             │
│  ├─ Create Pull Request                     │
│  ├─ Describe changes                        │
│  ├─ Wait for review                         │
│  └─ Address feedback                        │
└──────────────┬──────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│  8. Merge & Deploy                          │
│  ├─ Merge to main branch                    │
│  ├─ CI/CD pipeline runs                     │
│  └─ Deploy to staging                       │
└─────────────────────────────────────────────┘
```

## 🚀 Deployment Pipeline

```
Code Repository (GitHub)
        │
        ├─ Code Push
        │
        ▼
GitHub Actions CI/CD
    ├─ Run Tests ──→ ✓/✗
    ├─ Lint Code  ──→ ✓/✗
    ├─ Build Images
    └─ Push to Registry
        │
        ▼
Docker Registry
        │
        ├─ Store Images
        │
        ▼
Cloud Provider (AWS/Azure/GCP)
        │
        ├─ Pull Images
        ├─ Deploy Services
        ├─ Health Checks
        └─ Monitor
        │
        ▼
Production Environment
    ├─ Load Balancer
    ├─ Backend Services
    ├─ Frontend App
    └─ ML Engine
```

## 📱 Component Hierarchy

```
App (App.jsx)
│
└── Layout
    ├── Sidebar
    │   ├── Logo
    │   ├── NavLinks
    │   └── QuickStats
    │
    ├── Navbar
    │   ├── NotificationBell
    │   ├── Settings
    │   └── UserMenu
    │
    └── MainContent
        ├── Dashboard (/)
        │   ├── MetricsGrid
        │   ├── RiskChart
        │   ├── VulnChart
        │   └── ActivityFeed
        │
        ├── Projects (/projects)
        │   ├── ProjectCard (x3)
        │   └── AddProjectBtn
        │
        ├── ProjectDetail (/projects/:id)
        │   ├── ProjectInfo
        │   ├── ScanHistory
        │   └── RiskSummary
        │
        ├── Scans (/scans)
        │   └── ScansTable
        │
        ├── Risks (/risks)
        │   └── RisksTable
        │
        └── Pipelines (/pipelines)
            └── PipelineCards
```

## 🗄️ Database Schema

```
Projects Collection
├── _id (ObjectId)
├── name (String)
├── description (String)
├── repositoryUrl (String)
├── language (String)
├── riskScore (Number)
├── status (String)
└── createdAt (Date)

Scans Collection
├── _id (ObjectId)
├── projectId (Ref: Projects)
├── scanType (String)
├── status (String)
├── progress (Number)
├── findings (Array)
├── startedAt (Date)
├── completedAt (Date)
└── createdAt (Date)

Risks Collection
├── _id (ObjectId)
├── projectId (Ref: Projects)
├── title (String)
├── severity (String)
├── riskScore (Number)
├── file (String)
├── line (Number)
├── status (String)
└── createdAt (Date)
```

## 🔐 Authentication Flow

```
┌──────────────┐
│  Login Page  │
└──────┬───────┘
       │
       │ Email & Password
       │
       ▼
┌──────────────────────┐
│  Backend API         │
│  /api/auth/login     │
└──────┬───────────────┘
       │
       │ Validate Credentials
       │
       ▼
┌──────────────────────┐
│  JWT Token Generated │
│  & Returned          │
└──────┬───────────────┘
       │
       │ Store in LocalStorage
       │
       ▼
┌──────────────────────┐
│  All API Requests    │
│  Include JWT Token   │
│  in Header           │
└──────┬───────────────┘
       │
       │ Authorization Header
       │
       ▼
┌──────────────────────┐
│  Backend Middleware  │
│  Validates Token     │
└──────┬───────────────┘
       │
       ├─ Valid ──→ Proceed
       │
       └─ Invalid → 401 Error
```

## 📈 Risk Scoring Algorithm

```
Input: Code Analysis Data
   ├─ Vulnerabilities Found
   ├─ Dependencies Checked
   └─ Configurations Analyzed

Process:
   ├─ Extract Vulnerabilities
   │
   ├─ Assign Weights
   │  ├─ SQL Injection: 10
   │  ├─ Auth Bypass: 9
   │  ├─ XSS: 8
   │  ├─ Data Exposure: 8
   │  ├─ Weak Encryption: 7
   │  ├─ Insecure Deps: 6
   │  ├─ Misconfiguration: 5
   │  └─ Missing CORS: 4
   │
   ├─ Count Violations
   │
   ├─ Calculate Score
   │  Formula: Σ(weight × count) / 10
   │
   ├─ Normalize (0-10 scale)
   │
   └─ Classify Severity

Output:
   ├─ Risk Score: 0-10
   ├─ Severity Level
   │  ├─ CRITICAL (≥8)
   │  ├─ HIGH (6-7)
   │  ├─ MEDIUM (4-5)
   │  └─ LOW (<4)
   │
   └─ Recommendations
```

## 🔄 Service Communication

```
Frontend (React)
       │
       │ HTTP/HTTPS
       │
       ▼
Backend API (Express)
       │
       ├─ Database Queries
       │  │
       │  ▼
       │ MongoDB
       │
       ├─ ML Processing
       │  │
       │  ▼
       │ ML Engine (Flask)
       │
       └─ Cache Operations
          │
          ▼
          Redis

VS Code Extension
       │
       │ HTTP Requests
       │
       ▼
Backend API (Express)
```

## 📊 Deployment Environments

```
┌─────────────────────────────────────────┐
│  Development (Local)                    │
│  ├─ localhost:3000 (Frontend)           │
│  ├─ localhost:5000 (Backend)            │
│  └─ localhost:5001 (ML Engine)          │
└─────────────────────────────────────────┘
                    │
                    │ (Test & Commit)
                    │
                    ▼
┌─────────────────────────────────────────┐
│  Staging (Pre-Production)               │
│  ├─ Automated Testing                   │
│  ├─ Performance Testing                 │
│  └─ Security Scanning                   │
└─────────────────────────────────────────┘
                    │
                    │ (Approve & Deploy)
                    │
                    ▼
┌─────────────────────────────────────────┐
│  Production (Live)                      │
│  ├─ Load Balanced                       │
│  ├─ High Availability                   │
│  ├─ Monitoring & Alerting               │
│  └─ Backup & Recovery                   │
└─────────────────────────────────────────┘
```

---

**This visual guide helps you understand the project structure and workflows at a glance!**
